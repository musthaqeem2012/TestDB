#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('connect') {
            steps {
                echo "Hello World!"
 //def dbInvokeMethod = load "dbconnection.groovy"
			
	    	    
            }
        }
    }
}

